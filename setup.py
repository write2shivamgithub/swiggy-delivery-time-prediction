from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Builf ML project that predicts food delivery time in minutes',
    author='Shivam',
    license='',
)
